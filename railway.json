{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "if [ \"$RAILWAY_SERVICE_NAME\" = \"web\" ]; then pnpm --filter @touchline/web build; else echo worker needs no build; fi"
  },
  "deploy": {
    "startCommand": "if [ \"$RAILWAY_SERVICE_NAME\" = \"web\" ]; then pnpm --filter @touchline/web start; else pnpm --filter @touchline/worker start; fi",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}